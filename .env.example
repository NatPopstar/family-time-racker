# Образец файла .env.local — этот попадает в git, чтобы было видно,
# какие переменные вообще нужны проекту. Настоящие значения живут
# в .env.local, который в git не попадает.

VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_ANON_KEY=<ключ, который печатает команда `npx supabase status`>
