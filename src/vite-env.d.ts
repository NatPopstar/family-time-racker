/// <reference types="vite/client" />

// Эта строка объясняет TypeScript, что Vite умеет импортировать не только код,
// но и стили и картинки: import './index.css', import logo from './logo.svg'.
// Без неё TypeScript ругается «не знаю, что такое .css».
